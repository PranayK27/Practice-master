package interview.DSA;

public class GreedyAlgo {
}

class Algo{
//    void primMST(int graph[][]) {
//        int parent[] = new int[V];
//        int key[] = new int[V];
//        Boolean mstSet[] = new Boolean[V]; for (int i = 0; i < V; i++) {
//            key[i] = Integer.MAX_VALUE;
//            mstSet[i] = false; }
//        key[0] = 0; // Make key 0 so that this vertex is parent[0] = -1; // First node is always root of MST for (int count = 0; count < V - 1; count++) {
//        int u = minKey(key, mstSet); mstSet[u] = true;
//        for (int v = 0; v < V; v++)
//            if (graph[u][v] != 0 && mstSet[v] == false && graph[u][v] < key[v]) {
//                parent[v] = u;
//                key[v] = graph[u][v];
//            } }
//    printMST(parent, graph); }
}
